//! `SeaORM` Entity, @generated by sea-orm-codegen 1.0.0

use sea_orm::entity::prelude::*;
use serde::{Deserialize, Serialize};
use rust_decimal::Decimal;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "ty_store_combination")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub id: u32,
    pub product_id: u32,
    pub mer_id: Option<u32>,
    pub image: String,
    pub images: String,
    pub title: String,
    pub attr: Option<String>,
    pub people: u32,
    pub info: String,
    pub price: Decimal,
    pub sort: u32,
    pub sales: u32,
    pub stock: u32,
    pub add_time: i64,
    pub is_host: u32,
    pub is_show: u32,
    pub is_del: u32,
    pub combination: Option<u32>,
    pub mer_use: Option<u32>,
    pub is_postage: Option<u32>,
    pub postage: Option<Decimal>,
    pub start_time: u64,
    pub stop_time: u64,
    pub effective_time: i32,
    pub cost: Decimal,
    pub browse: Option<i32>,
    pub unit_name: String,
    pub temp_id: i32,
    pub weight: Option<Decimal>,
    pub volume: Option<Decimal>,
    pub num: Option<i32>,
    pub quota: i32,
    pub quota_show: i32,
    pub ot_price: Decimal,
    pub once_num: i32,
    pub virtual_ration: i32,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {}

impl ActiveModelBehavior for ActiveModel {}
