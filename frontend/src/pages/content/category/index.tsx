import React from 'react';
import Category from '@/components/Category';

const ContentCategory: React.FC = () => {
  return <Category biztype={{ value: 3, name: '文章分类' }} />;
};

export default ContentCategory;
