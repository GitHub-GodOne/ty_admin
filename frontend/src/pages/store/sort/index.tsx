import React from 'react';
import Category from '@/components/Category';

const StoreSort: React.FC = () => {
  return <Category biztype={{ value: 1, name: '商品分类' }} />;
};

export default StoreSort;
